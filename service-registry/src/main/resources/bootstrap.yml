spring:
  application:
    name: service-registry
  cloud:
    config:
      failFast: true
      uri: http://service-config:8888
      password: test
      username: user

eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: false
    fetchRegistry: false
    server:
      waitTimeInMsWhenSyncEmpty: 0

